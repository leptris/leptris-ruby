# Every platform checks out identical bytes: Windows runners default
# to core.autocrlf, which rewrote lib/**/*.rb to CRLF and the
# publish-time Ruby-source parity gate correctly refused the release
# (the mingw variants shipped CRLF while every other platform shipped
# LF). Normalize all text to LF on checkout and in the index.
* text=auto eol=lf
